#summary One-sentence summary of this page.

= Basic Example =

Made to do what the "[http://code.google.com/p/android-market-api/ Basic Java example]" does in the android-market-api project

{{{
include("../local.php");
include("../proto/protocolbuffers.inc.php");
include("../proto/market.proto.php");
include("../Market/MarketSession.php");

$session = new MarketSession();
$session->login(GOOGLE_EMAIL, GOOGLE_PASSWD);

$ar = new AppsRequest();
$ar->setQuery("maps");
$ar->setStartIndex(0);
$ar->setEntriesCount(10);
$ar->setWithExtendedInfo(true);

$reqGroup = new Request_RequestGroup();
$reqGroup->setAppsRequest($ar);

$response = $session->execute($reqGroup);
}}}

= Examples =

HowToSearchApps
HowToGetAppComments
HowToGetAppScreenshot 